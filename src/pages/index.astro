---
import { ViewTransitions } from 'astro:transitions';
import HomeLayout from '../layouts/HomeLayout.astro';
---

<script>
	let stat1Num = document.querySelector('.stat1 .num');
	let stat3Num = document.querySelector('.stat3 .num');
	let interval = 1000;

	const animateNumber = (
		numElement: HTMLElement,
		startValue: number,
		endValue: number,
		increment: number,
	) => {
		let duration = Math.floor(interval / endValue);
		let counter = setInterval(() => {
			startValue += increment;
			numElement.textContent = numElement.closest('.stat1')
				? `${startValue.toFixed(0)}%.`
				: startValue.toFixed(1);
			if (startValue >= endValue) clearInterval(counter);
		}, duration);
	};

	const observerStat1 = new IntersectionObserver(
		entries => {
			const entry = entries[0]; // access the first (and only) entry directly
			if (entry.isIntersecting) {
				animateNumber(stat1Num as HTMLElement, 0, 73, 1);
				observerStat1.unobserve(entry.target); // stop observing after animation starts
			}
		},
		{
			threshold: 1, // animation starts once 100% of element's visible
			rootMargin: '50px', // animation starts 50px before element's visible
		},
	);

	const observerStat3 = new IntersectionObserver(
		entries => {
			const entry = entries[0];
			if (entry.isIntersecting) {
				animateNumber(stat3Num as HTMLElement, 0, 73.5, 3.5);
				observerStat3.unobserve(entry.target);
			}
		},
		{
			threshold: 1,
			rootMargin: '100px',
		},
	);

	observerStat1.observe(stat1Num as HTMLElement);
	observerStat3.observe(stat3Num as HTMLElement);
</script>

<HomeLayout title="BovEco - iGEM 2024">
	<ViewTransitions />
	<main>
		<div class="welcome-text">
			<img
				src="/waterloo/img/waterloo-igem-pixelated.png"
				alt="Waterloo iGEM Logo"
			/>
			<h3>Waterloo iGEM Presents...</h3>
			<h1 transition:animate="fade">BovEco</h1>
			<h2>
				Fighting <span>climate change</span>, one cow at a <span>time!</span>
			</h2>
		</div>
		<div class="stat1">
			<div class="num"></div>
			<div class="text">
				That's the proportion of agricultural <span>methane</span>
				emissions released by ruminants.
			</div>
		</div>
		<div class="stat2">
			<span>
				<b>One</b> cow produces over <b>100</b> kg of methane a year...
			</span>
			<img src="img/cows/burping-cow.gif" alt="Burping Cow" />
			<img src="img/cliff-edge.png" alt="Cliff Edge" />
			<img src="img/cliff-column.png" alt="Cliff Column" />
			<span>
				...now imagine close to <br /><b>1.5 billion</b> cows globally.
			</span>
		</div>
		<div class="stat3">
			that is
			<div class="num"></div>
			<div class="text">
				million metric tons of enteric methane produced by cattle—globally.
			</div>
		</div>
		<div class="fact1">
			<div>As the global population increases,</div>
			<div>so will the demand for beef and dairy products.</div>
		</div>
		<div class="fact2">
			<p>Methane emissions drive serious environmental issues:</p>
			<p>
				accelerating global temperatures, dragrading air quality, and
				threatening crop growth--all of which impact human and animal health.
			</p>
		</div>
		<div class="fact3">
			Addressing these CH4 issues is crucial for a healthier planet.
		</div>
		<div class="rationale">
			<div>So why tackle CH4?</div>
			<img src="" alt="" />
			<div>
				Methane has a shorter atmospheric life of 7-12 years compared to other
				long-lasting GHGs.
			</div>
			<div>
				Reducing methane emissions can lead to quicker reductions in global
				warming and atmospheric GHG levels.
			</div>
		</div>
		<div class="proposal">
			<div>So what if...</div>
			<div>GMO plants used to feed cows could be modified to</div>
			<div>solve methane problem?</div>
		</div>
		<div class="bottom">
			<!-- <img src="img/ocean.png" alt="" /> -->
		</div>
	</main>
</HomeLayout>

<style lang="scss">
	@import '../global.scss';
	main {
		.welcome-text {
			display: flex;
			flex-direction: column;
			align-items: center;
			color: $accent-1;
			margin-top: 2.5rem;
			z-index: -50;

			img {
				width: 10rem;
				filter: hue-rotate(275deg) saturate(50%);
				margin-bottom: 0.75rem;
			}

			h1 {
				font-family: 'Quartzo', sans-serif;
				font-size: 10rem;
				margin-top: -0.5rem;
			}

			h2 {
				font-size: 2.25rem;

				span:nth-of-type(1) {
					color: $accent-4;
				}
				span:nth-of-type(2) {
					box-shadow: 25px 15px 1px $accent-1;
				}
			}

			h3 {
				font-size: 2rem;
			}
		}

		.stat1 {
			display: flex;
			justify-content: center;
			align-items: center;
			padding: 3rem;
			font-weight: bold;
			background: $accent-1;
			color: $primary;
			margin-top: 30rem;

			.num {
				min-width: 4ch; // avoid layout shift
				font-size: 15rem;
				margin-bottom: -2.5rem;
			}

			.text {
				font-size: 3rem;

				span {
					color: $accent-3;
				}
			}
		}

		.stat2 {
			display: flex;
			flex-direction: column;
			align-items: flex-start;
			font-size: 4rem;
			margin-top: 30rem;

			span {
				width: 50%;
				margin-top: 5rem;
				margin-left: 5rem;
				&:nth-of-type(2) {
					margin-top: 2rem;
				}
			}

			img {
				position: relative;
				&:nth-of-type(1) {
					width: 35rem;
					margin-top: 5rem;
					position: absolute;
					right: 0;
				}
				&:nth-of-type(2) {
					// width: 80%;
					opacity: 0.9;
					margin-top: 15rem;
					z-index: -5;
					position: absolute;
					right: 0;
				}

				&:nth-of-type(3) {
					// width: 80%;
					opacity: 0.9;
					margin-top: 41rem;
					z-index: -5;
					position: absolute;
					right: -3rem;
				}
			}
		}

		.stat3 {
			display: flex;
			flex-direction: column;
			justify-content: center;
			align-items: center;
			text-align: center;
			font-size: 2.5rem;
			margin-top: 20rem;
			font-weight: bold;

			.num {
				font-size: 18rem;
				margin-top: 1.5rem;
				margin-bottom: -1rem;
			}

			.text {
				width: 60%;
			}
		}

		.fact1 {
			display: flex;
			flex-direction: column;
			justify-content: center;
			align-items: center;
			font-size: 3rem;
			margin-top: 20rem;

			div {
				margin-bottom: 1rem;
			}
		}

		.fact2 {
			display: flex;
			flex-direction: column;
			justify-content: center;
			align-items: center;
			font-size: 3rem;
			margin-top: 20rem;

			p {
				margin-bottom: 1rem;
			}
		}

		.fact3 {
			display: flex;
			justify-content: center;
			align-items: center;
			font-size: 3rem;
			margin-top: 20rem;
		}

		.rationale {
			display: flex;
			flex-direction: column;
			justify-content: center;
			align-items: center;
			font-size: 3rem;
			margin-top: 20rem;

			div {
				margin-bottom: 1rem;
			}
		}

		.proposal {
			display: flex;
			flex-direction: column;
			justify-content: center;
			align-items: center;
			font-size: 3rem;
			margin-top: 20rem;

			div {
				margin-bottom: 1rem;
			}
		}

		.bottom {
			display: flex;
			justify-content: center;
			align-items: center;
			margin-top: 20rem;
		}
	}
</style>
