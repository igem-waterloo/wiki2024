<script>
  const button = document.querySelector('button');
  document.addEventListener('click', e => {
    // Handle button click here (instead of <a> tag) to avoid loading screen issues
    if (e.target === button) window.location.href = '/waterloo/';
  });
</script>
<div class="bkgd">
  <div class="text">
    <h1>404</h1>
    <p>Oops... page not found.</p>
    <button>Home</button>
  </div>
  <img
    src="https://static.igem.wiki/teams/5118/img/cows/walking-cow.webp"
    class="walking-cow"
    alt="Walking Cow"
  />
  <img
    src="https://static.igem.wiki/teams/5118/img/cows/walking-cow.webp"
    class="walking-cow"
    alt="Walking Cow"
  />
  <img
    src="https://static.igem.wiki/teams/5118/img/cows/walking-cow.webp"
    class="walking-cow"
    alt="Walking Cow"
  />
  <img
    src="https://static.igem.wiki/teams/5118/img/environment/grass.webp"
    class="grass"
    alt="Grass"
  />
</div>

<style lang="scss">
  @import '../global.scss';

  .bkgd {
    width: 100%;
    height: 100%;
    background: radial-gradient($primary 20%, $accent-3 80%, $accent-4);
    transition: opacity 0.5s ease;
    position: fixed;
    z-index: 100;
    top: 0;
    left: 0;

    .text {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      color: $accent-2;
      position: relative;
      top: 7rem;
      left: 4rem;

      h1 {
        font-size: 8rem;
      }

      p {
        font-size: 2.5rem;
      }

      button {
        font-size: 2rem;
        padding: 0.5rem 1rem;
        margin-top: 1rem;
        background: $accent-2;
        color: $primary;
        border: none;
        border-radius: 0.5rem;
        box-shadow: 5px -10px 0 rgba($accent-2, 0.5);
        margin-top: 2.25rem;
        cursor: pointer;

        &:hover {
          background: $accent-2;
          @include transition-ease;
        }

        &:active {
          transform: scale(0.95);
          box-shadow: 5px -4px 0 rgba($accent-2, 0.5);
        }
      }
    }

    .walking-cow {
      width: 300px;
      height: 300px;
      position: absolute;
      bottom: 1rem;
      left: 15rem;
      animation: move-cows-across-screen 60s infinite linear;

      &:nth-of-type(2) {
        bottom: 8.5rem;
      }

      &:nth-of-type(3) {
        bottom: 16rem;
      }
    }
  }

  .grass {
    width: 100%;
    height: 10rem;
    position: absolute;
    bottom: 0;
    z-index: -1;
    image-rendering: pixelated;
    filter: hue-rotate(35deg) saturate(55%) brightness(80%);
  }

  @keyframes move-cows-across-screen {
    0% {
      transform: translateX(0vw);
    }
    100% {
      transform: translateX(100vw);
    }
  }
</style>
