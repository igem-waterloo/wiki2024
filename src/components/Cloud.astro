---
// const foregroundCloud = document.querySelector(".foreground").innerHTML = Math.random();
// const backgroundCloud =
---

<img class="foreground" src="img/cloud.png" alt="" />
<img class="background offscreen" src="img/cloud.png" alt="" />

<style lang="scss">
  @import '../global.scss';

  .foreground {
    width: 100px;
    opacity: 0.7;
    z-index: 1;
    overflow-x: hidden;
    position: absolute;
    animation: foreground 40s linear infinite;
  }

  .background {
    width: 200px;
    opacity: 0.4;
    z-index: -1;
    position: absolute;
    left: -5rem;
    animation: background 70s linear infinite;
  }

  @keyframes foreground {
    0% {
      // transform: translateX(-5rem);
    }
    // 50% {
    //   transform: translateY(50px);
    // }
    99% {
      transform: translateX(100vw);
      // opacity: 0;
    }
    100% {
      // transform: translateX(100vw);
      opacity: 0;
    }
  }

  @keyframes background {
    0% {
      transform: translateX(-5rem);
    }
    // 50% {
    //   transform: translateY(-70px);
    // }
    100% {
      transform: translateX(100vw);
      opacity: 0;
    }
  }
</style>
