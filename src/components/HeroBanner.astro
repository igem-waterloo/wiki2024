---
interface Props {
  title: string;
  heading?: string;
  layout: string;
}

const { title, heading, layout } = Astro.props;

let bkgdColor = '';
if (layout === 'lnd')
  bkgdColor = 'rgb(151, 200, 216)'; // $lnd
else if (layout === 'mnm')
  bkgdColor = 'rgb(146, 81, 146)'; // $mnm
else bkgdColor = 'rgb(99, 183, 144);'; // $hp
---

<div id="hero-banner">
  <h1>{title}</h1>
  {heading && <h2>{heading}</h2>}
</div>

<style lang="scss" define:vars={{ bkgdColor: bkgdColor }}>
  @import '../global.scss';

  #hero-banner {
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: var(--bkgdColor);
    color: $primary;
    opacity: 0.8;
    padding: 2rem;
    margin-top: -3rem;

    h1 {
      font-size: 2rem;
    }

    h2 {
      font-size: 1.5rem;
    }

    p {
      font-size: 1.25rem;
    }

    img {
      object-fit: contain;
      width: 100%;
      z-index: -10;
      position: absolute;
      border-radius: 1rem;
    }
  }
</style>
